class UserMailer < ActionMailer::Base
  default :from => "admin@pochemu-ya-takaya-dura.ru"
  def createsto<%= @user.name %>,

Thank you for registering!

Edit profile: <%= edit_user_url(@user) %>

<!-- views/user_mailer/registration_confirmation.html.erb -->
<p><%= @user.name %>,</p>

<p>Thank you for registering!</p>

<p><%= link_to "Edit profile", edit_user_url(@user) %></p>ry_confirmation(story)
    @story = story
    User.all.each do user
      @user = user
      mail :to => user.mail, :subject => "New story added"
    end
  end
end
